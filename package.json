{
  "name": "@fezchat/cant",
  "version": "0.4.0",
  "description": "Connect your existing AI agents across harnesses and machines. Encrypted Nostr DMs, Jev coordination, and a local conversation viewer.",
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "cant": "dist/cli.js",
    "sidecar": "dist/cli.js"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "check": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/cli.js",
    "prepublishOnly": "npm test && npm run build"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.5.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "nostr-tools": "^2.24.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KennethAshley/sidecar.git"
  },
  "keywords": [
    "cant",
    "jev",
    "nostr",
    "typescript",
    "agents",
    "fez",
    "mcp",
    "acp"
  ],
  "homepage": "https://fez.chat"
}
